/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CashDrawerShiftState {
    public static final CashDrawerShiftState CLOSED = new CashDrawerShiftState(Value.CLOSED, "CLOSED");

    public static final CashDrawerShiftState OPEN = new CashDrawerShiftState(Value.OPEN, "OPEN");

    public static final CashDrawerShiftState ENDED = new CashDrawerShiftState(Value.ENDED, "ENDED");

    private final Value value;

    private final String string;

    CashDrawerShiftState(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CashDrawerShiftState && this.string.equals(((CashDrawerShiftState) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case CLOSED:
                return visitor.visitClosed();
            case OPEN:
                return visitor.visitOpen();
            case ENDED:
                return visitor.visitEnded();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CashDrawerShiftState valueOf(String value) {
        switch (value) {
            case "CLOSED":
                return CLOSED;
            case "OPEN":
                return OPEN;
            case "ENDED":
                return ENDED;
            default:
                return new CashDrawerShiftState(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        OPEN,

        ENDED,

        CLOSED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitOpen();

        T visitEnded();

        T visitClosed();

        T visitUnknown(String unknownType);
    }
}
