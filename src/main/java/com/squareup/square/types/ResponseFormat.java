/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ResponseFormat {
    public static final ResponseFormat COLUMNAR = new ResponseFormat(Value.COLUMNAR, "columnar");

    public static final ResponseFormat DEFAULT = new ResponseFormat(Value.DEFAULT, "default");

    public static final ResponseFormat COMPACT = new ResponseFormat(Value.COMPACT, "compact");

    private final Value value;

    private final String string;

    ResponseFormat(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof ResponseFormat && this.string.equals(((ResponseFormat) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case COLUMNAR:
                return visitor.visitColumnar();
            case DEFAULT:
                return visitor.visitDefault();
            case COMPACT:
                return visitor.visitCompact();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ResponseFormat valueOf(String value) {
        switch (value) {
            case "columnar":
                return COLUMNAR;
            case "default":
                return DEFAULT;
            case "compact":
                return COMPACT;
            default:
                return new ResponseFormat(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        DEFAULT,

        COMPACT,

        COLUMNAR,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitDefault();

        T visitCompact();

        T visitColumnar();

        T visitUnknown(String unknownType);
    }
}
