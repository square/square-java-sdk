/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class GiftCardActivityAdjustIncrementReason {
    public static final GiftCardActivityAdjustIncrementReason COMPLIMENTARY =
            new GiftCardActivityAdjustIncrementReason(Value.COMPLIMENTARY, "COMPLIMENTARY");

    public static final GiftCardActivityAdjustIncrementReason SUPPORT_ISSUE =
            new GiftCardActivityAdjustIncrementReason(Value.SUPPORT_ISSUE, "SUPPORT_ISSUE");

    public static final GiftCardActivityAdjustIncrementReason TRANSACTION_VOIDED =
            new GiftCardActivityAdjustIncrementReason(Value.TRANSACTION_VOIDED, "TRANSACTION_VOIDED");

    private final Value value;

    private final String string;

    GiftCardActivityAdjustIncrementReason(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof GiftCardActivityAdjustIncrementReason
                        && this.string.equals(((GiftCardActivityAdjustIncrementReason) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case COMPLIMENTARY:
                return visitor.visitComplimentary();
            case SUPPORT_ISSUE:
                return visitor.visitSupportIssue();
            case TRANSACTION_VOIDED:
                return visitor.visitTransactionVoided();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static GiftCardActivityAdjustIncrementReason valueOf(String value) {
        switch (value) {
            case "COMPLIMENTARY":
                return COMPLIMENTARY;
            case "SUPPORT_ISSUE":
                return SUPPORT_ISSUE;
            case "TRANSACTION_VOIDED":
                return TRANSACTION_VOIDED;
            default:
                return new GiftCardActivityAdjustIncrementReason(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        COMPLIMENTARY,

        SUPPORT_ISSUE,

        TRANSACTION_VOIDED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitComplimentary();

        T visitSupportIssue();

        T visitTransactionVoided();

        T visitUnknown(String unknownType);
    }
}
