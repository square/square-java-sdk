/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CacheMode {
    public static final CacheMode MUST_REVALIDATE = new CacheMode(Value.MUST_REVALIDATE, "must-revalidate");

    public static final CacheMode NO_CACHE = new CacheMode(Value.NO_CACHE, "no-cache");

    public static final CacheMode STALE_WHILE_REVALIDATE =
            new CacheMode(Value.STALE_WHILE_REVALIDATE, "stale-while-revalidate");

    public static final CacheMode STALE_IF_SLOW = new CacheMode(Value.STALE_IF_SLOW, "stale-if-slow");

    private final Value value;

    private final String string;

    CacheMode(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof CacheMode && this.string.equals(((CacheMode) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case MUST_REVALIDATE:
                return visitor.visitMustRevalidate();
            case NO_CACHE:
                return visitor.visitNoCache();
            case STALE_WHILE_REVALIDATE:
                return visitor.visitStaleWhileRevalidate();
            case STALE_IF_SLOW:
                return visitor.visitStaleIfSlow();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CacheMode valueOf(String value) {
        switch (value) {
            case "must-revalidate":
                return MUST_REVALIDATE;
            case "no-cache":
                return NO_CACHE;
            case "stale-while-revalidate":
                return STALE_WHILE_REVALIDATE;
            case "stale-if-slow":
                return STALE_IF_SLOW;
            default:
                return new CacheMode(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        STALE_IF_SLOW,

        STALE_WHILE_REVALIDATE,

        MUST_REVALIDATE,

        NO_CACHE,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitStaleIfSlow();

        T visitStaleWhileRevalidate();

        T visitMustRevalidate();

        T visitNoCache();

        T visitUnknown(String unknownType);
    }
}
