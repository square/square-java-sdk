/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class BankAccountStatus {
    public static final BankAccountStatus VERIFIED = new BankAccountStatus(Value.VERIFIED, "VERIFIED");

    public static final BankAccountStatus DISABLED = new BankAccountStatus(Value.DISABLED, "DISABLED");

    public static final BankAccountStatus VERIFICATION_IN_PROGRESS =
            new BankAccountStatus(Value.VERIFICATION_IN_PROGRESS, "VERIFICATION_IN_PROGRESS");

    private final Value value;

    private final String string;

    BankAccountStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof BankAccountStatus && this.string.equals(((BankAccountStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case VERIFIED:
                return visitor.visitVerified();
            case DISABLED:
                return visitor.visitDisabled();
            case VERIFICATION_IN_PROGRESS:
                return visitor.visitVerificationInProgress();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static BankAccountStatus valueOf(String value) {
        switch (value) {
            case "VERIFIED":
                return VERIFIED;
            case "DISABLED":
                return DISABLED;
            case "VERIFICATION_IN_PROGRESS":
                return VERIFICATION_IN_PROGRESS;
            default:
                return new BankAccountStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        VERIFICATION_IN_PROGRESS,

        VERIFIED,

        DISABLED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitVerificationInProgress();

        T visitVerified();

        T visitDisabled();

        T visitUnknown(String unknownType);
    }
}
