/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class LoyaltyPromotionStatus {
    public static final LoyaltyPromotionStatus CANCELED = new LoyaltyPromotionStatus(Value.CANCELED, "CANCELED");

    public static final LoyaltyPromotionStatus SCHEDULED = new LoyaltyPromotionStatus(Value.SCHEDULED, "SCHEDULED");

    public static final LoyaltyPromotionStatus ACTIVE = new LoyaltyPromotionStatus(Value.ACTIVE, "ACTIVE");

    public static final LoyaltyPromotionStatus ENDED = new LoyaltyPromotionStatus(Value.ENDED, "ENDED");

    private final Value value;

    private final String string;

    LoyaltyPromotionStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof LoyaltyPromotionStatus
                        && this.string.equals(((LoyaltyPromotionStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case CANCELED:
                return visitor.visitCanceled();
            case SCHEDULED:
                return visitor.visitScheduled();
            case ACTIVE:
                return visitor.visitActive();
            case ENDED:
                return visitor.visitEnded();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static LoyaltyPromotionStatus valueOf(String value) {
        switch (value) {
            case "CANCELED":
                return CANCELED;
            case "SCHEDULED":
                return SCHEDULED;
            case "ACTIVE":
                return ACTIVE;
            case "ENDED":
                return ENDED;
            default:
                return new LoyaltyPromotionStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ACTIVE,

        ENDED,

        CANCELED,

        SCHEDULED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitActive();

        T visitEnded();

        T visitCanceled();

        T visitScheduled();

        T visitUnknown(String unknownType);
    }
}
