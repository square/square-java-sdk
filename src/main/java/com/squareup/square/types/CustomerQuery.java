/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.squareup.square.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CustomerQuery.Builder.class)
public final class CustomerQuery {
    private final Optional<CustomerFilter> filter;

    private final Optional<CustomerSort> sort;

    private final Map<String, Object> additionalProperties;

    private CustomerQuery(
            Optional<CustomerFilter> filter, Optional<CustomerSort> sort, Map<String, Object> additionalProperties) {
        this.filter = filter;
        this.sort = sort;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The filtering criteria for the search query. A query can contain multiple filters in any combination.
     * Multiple filters are combined as <code>AND</code> statements.
     * <p><strong>Note:</strong> Combining multiple filters as <code>OR</code> statements is not supported. Instead, send multiple single-filter
     * searches and join the result sets.</p>
     */
    @JsonProperty("filter")
    public Optional<CustomerFilter> getFilter() {
        return filter;
    }

    /**
     * @return Sorting criteria for query results. The default behavior is to sort
     * customers alphabetically by <code>given_name</code> and <code>family_name</code>.
     */
    @JsonProperty("sort")
    public Optional<CustomerSort> getSort() {
        return sort;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CustomerQuery && equalTo((CustomerQuery) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CustomerQuery other) {
        return filter.equals(other.filter) && sort.equals(other.sort);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.filter, this.sort);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<CustomerFilter> filter = Optional.empty();

        private Optional<CustomerSort> sort = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(CustomerQuery other) {
            filter(other.getFilter());
            sort(other.getSort());
            return this;
        }

        /**
         * <p>The filtering criteria for the search query. A query can contain multiple filters in any combination.
         * Multiple filters are combined as <code>AND</code> statements.</p>
         * <p><strong>Note:</strong> Combining multiple filters as <code>OR</code> statements is not supported. Instead, send multiple single-filter
         * searches and join the result sets.</p>
         */
        @JsonSetter(value = "filter", nulls = Nulls.SKIP)
        public Builder filter(Optional<CustomerFilter> filter) {
            this.filter = filter;
            return this;
        }

        public Builder filter(CustomerFilter filter) {
            this.filter = Optional.ofNullable(filter);
            return this;
        }

        /**
         * <p>Sorting criteria for query results. The default behavior is to sort
         * customers alphabetically by <code>given_name</code> and <code>family_name</code>.</p>
         */
        @JsonSetter(value = "sort", nulls = Nulls.SKIP)
        public Builder sort(Optional<CustomerSort> sort) {
            this.sort = sort;
            return this;
        }

        public Builder sort(CustomerSort sort) {
            this.sort = Optional.ofNullable(sort);
            return this;
        }

        public CustomerQuery build() {
            return new CustomerQuery(filter, sort, additionalProperties);
        }
    }
}
