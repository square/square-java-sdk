/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.types;

import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonParseException;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.squareup.square.core.ObjectMappers;
import java.io.IOException;
import java.util.Objects;

@JsonDeserialize(using = Format.Deserializer.class)
public final class Format {
    private final Object value;

    private final int type;

    private Format(Object value, int type) {
        this.value = value;
        this.type = type;
    }

    @JsonValue
    public Object get() {
        return this.value;
    }

    @SuppressWarnings("unchecked")
    public <T> T visit(Visitor<T> visitor) {
        if (this.type == 0) {
            return visitor.visit((SimpleFormat) this.value);
        } else if (this.type == 1) {
            return visitor.visit((LinkFormat) this.value);
        } else if (this.type == 2) {
            return visitor.visit((CustomTimeFormat) this.value);
        } else if (this.type == 3) {
            return visitor.visit((CustomNumericFormat) this.value);
        }
        throw new IllegalStateException("Failed to visit value. This should never happen.");
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof Format && equalTo((Format) other);
    }

    private boolean equalTo(Format other) {
        return value.equals(other.value);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.value);
    }

    @java.lang.Override
    public String toString() {
        return this.value.toString();
    }

    public static Format of(SimpleFormat value) {
        return new Format(value, 0);
    }

    public static Format of(LinkFormat value) {
        return new Format(value, 1);
    }

    public static Format of(CustomTimeFormat value) {
        return new Format(value, 2);
    }

    public static Format of(CustomNumericFormat value) {
        return new Format(value, 3);
    }

    public interface Visitor<T> {
        T visit(SimpleFormat value);

        T visit(LinkFormat value);

        T visit(CustomTimeFormat value);

        T visit(CustomNumericFormat value);
    }

    static final class Deserializer extends StdDeserializer<Format> {
        Deserializer() {
            super(Format.class);
        }

        @java.lang.Override
        public Format deserialize(JsonParser p, DeserializationContext context) throws IOException {
            Object value = p.readValueAs(Object.class);
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, SimpleFormat.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, LinkFormat.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, CustomTimeFormat.class));
            } catch (RuntimeException e) {
            }
            try {
                return of(ObjectMappers.JSON_MAPPER.convertValue(value, CustomNumericFormat.class));
            } catch (RuntimeException e) {
            }
            throw new JsonParseException(p, "Failed to deserialize");
        }
    }
}
