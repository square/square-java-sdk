/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.locations.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.squareup.square.core.ObjectMappers;
import com.squareup.square.types.CustomAttributeDefinition;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateLocationCustomAttributeDefinitionRequest.Builder.class)
public final class CreateLocationCustomAttributeDefinitionRequest {
    private final CustomAttributeDefinition customAttributeDefinition;

    private final Optional<String> idempotencyKey;

    private final Map<String, Object> additionalProperties;

    private CreateLocationCustomAttributeDefinitionRequest(
            CustomAttributeDefinition customAttributeDefinition,
            Optional<String> idempotencyKey,
            Map<String, Object> additionalProperties) {
        this.customAttributeDefinition = customAttributeDefinition;
        this.idempotencyKey = idempotencyKey;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The custom attribute definition to create. Note the following:
     * <ul>
     * <li>With the exception of the <code>Selection</code> data type, the <code>schema</code> is specified as a simple URL to the JSON schema
     * definition hosted on the Square CDN. For more information, including supported values and constraints, see
     * <a href="https://developer.squareup.com/docs/devtools/customattributes/overview#supported-data-types">Supported data types</a>.</li>
     * <li><code>name</code> is required unless <code>visibility</code> is set to <code>VISIBILITY_HIDDEN</code>.</li>
     * </ul>
     */
    @JsonProperty("custom_attribute_definition")
    public CustomAttributeDefinition getCustomAttributeDefinition() {
        return customAttributeDefinition;
    }

    /**
     * @return A unique identifier for this request, used to ensure idempotency. For more information,
     * see <a href="https://developer.squareup.com/docs/build-basics/common-api-patterns/idempotency">Idempotency</a>.
     */
    @JsonProperty("idempotency_key")
    public Optional<String> getIdempotencyKey() {
        return idempotencyKey;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateLocationCustomAttributeDefinitionRequest
                && equalTo((CreateLocationCustomAttributeDefinitionRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateLocationCustomAttributeDefinitionRequest other) {
        return customAttributeDefinition.equals(other.customAttributeDefinition)
                && idempotencyKey.equals(other.idempotencyKey);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.customAttributeDefinition, this.idempotencyKey);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static CustomAttributeDefinitionStage builder() {
        return new Builder();
    }

    public interface CustomAttributeDefinitionStage {
        /**
         * <p>The custom attribute definition to create. Note the following:</p>
         * <ul>
         * <li>With the exception of the <code>Selection</code> data type, the <code>schema</code> is specified as a simple URL to the JSON schema
         * definition hosted on the Square CDN. For more information, including supported values and constraints, see
         * <a href="https://developer.squareup.com/docs/devtools/customattributes/overview#supported-data-types">Supported data types</a>.</li>
         * <li><code>name</code> is required unless <code>visibility</code> is set to <code>VISIBILITY_HIDDEN</code>.</li>
         * </ul>
         */
        _FinalStage customAttributeDefinition(@NotNull CustomAttributeDefinition customAttributeDefinition);

        Builder from(CreateLocationCustomAttributeDefinitionRequest other);
    }

    public interface _FinalStage {
        CreateLocationCustomAttributeDefinitionRequest build();

        /**
         * <p>A unique identifier for this request, used to ensure idempotency. For more information,
         * see <a href="https://developer.squareup.com/docs/build-basics/common-api-patterns/idempotency">Idempotency</a>.</p>
         */
        _FinalStage idempotencyKey(Optional<String> idempotencyKey);

        _FinalStage idempotencyKey(String idempotencyKey);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements CustomAttributeDefinitionStage, _FinalStage {
        private CustomAttributeDefinition customAttributeDefinition;

        private Optional<String> idempotencyKey = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CreateLocationCustomAttributeDefinitionRequest other) {
            customAttributeDefinition(other.getCustomAttributeDefinition());
            idempotencyKey(other.getIdempotencyKey());
            return this;
        }

        /**
         * <p>The custom attribute definition to create. Note the following:</p>
         * <ul>
         * <li>With the exception of the <code>Selection</code> data type, the <code>schema</code> is specified as a simple URL to the JSON schema
         * definition hosted on the Square CDN. For more information, including supported values and constraints, see
         * <a href="https://developer.squareup.com/docs/devtools/customattributes/overview#supported-data-types">Supported data types</a>.</li>
         * <li><code>name</code> is required unless <code>visibility</code> is set to <code>VISIBILITY_HIDDEN</code>.</li>
         * </ul>
         * <p>The custom attribute definition to create. Note the following:</p>
         * <ul>
         * <li>With the exception of the <code>Selection</code> data type, the <code>schema</code> is specified as a simple URL to the JSON schema
         * definition hosted on the Square CDN. For more information, including supported values and constraints, see
         * <a href="https://developer.squareup.com/docs/devtools/customattributes/overview#supported-data-types">Supported data types</a>.</li>
         * <li><code>name</code> is required unless <code>visibility</code> is set to <code>VISIBILITY_HIDDEN</code>.</li>
         * </ul>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("custom_attribute_definition")
        public _FinalStage customAttributeDefinition(@NotNull CustomAttributeDefinition customAttributeDefinition) {
            this.customAttributeDefinition =
                    Objects.requireNonNull(customAttributeDefinition, "customAttributeDefinition must not be null");
            return this;
        }

        /**
         * <p>A unique identifier for this request, used to ensure idempotency. For more information,
         * see <a href="https://developer.squareup.com/docs/build-basics/common-api-patterns/idempotency">Idempotency</a>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage idempotencyKey(String idempotencyKey) {
            this.idempotencyKey = Optional.ofNullable(idempotencyKey);
            return this;
        }

        /**
         * <p>A unique identifier for this request, used to ensure idempotency. For more information,
         * see <a href="https://developer.squareup.com/docs/build-basics/common-api-patterns/idempotency">Idempotency</a>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "idempotency_key", nulls = Nulls.SKIP)
        public _FinalStage idempotencyKey(Optional<String> idempotencyKey) {
            this.idempotencyKey = idempotencyKey;
            return this;
        }

        @java.lang.Override
        public CreateLocationCustomAttributeDefinitionRequest build() {
            return new CreateLocationCustomAttributeDefinitionRequest(
                    customAttributeDefinition, idempotencyKey, additionalProperties);
        }
    }
}
