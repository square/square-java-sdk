/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square;

import com.squareup.square.core.ClientOptions;
import com.squareup.square.core.RequestOptions;
import com.squareup.square.types.LoadRequest;
import com.squareup.square.types.LoadResponse;
import com.squareup.square.types.MetadataResponse;

public class ReportingClient {
    protected final ClientOptions clientOptions;

    private final RawReportingClient rawClient;

    public ReportingClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawReportingClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawReportingClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Describes the data available to query: the cubes, views, measures, dimensions, and segments you can reference in a reporting query. Call this first to discover the schema, then pass the members you need to <code>load</code>.
     */
    public MetadataResponse getMetadata() {
        return this.rawClient.getMetadata().body();
    }

    /**
     * Describes the data available to query: the cubes, views, measures, dimensions, and segments you can reference in a reporting query. Call this first to discover the schema, then pass the members you need to <code>load</code>.
     */
    public MetadataResponse getMetadata(RequestOptions requestOptions) {
        return this.rawClient.getMetadata(requestOptions).body();
    }

    /**
     * Runs a reporting query against the discovered schema and returns the aggregated results. Long-running queries may return a &quot;Continue wait&quot; response while processing — retry the same request until results are ready.
     */
    public LoadResponse load() {
        return this.rawClient.load().body();
    }

    /**
     * Runs a reporting query against the discovered schema and returns the aggregated results. Long-running queries may return a &quot;Continue wait&quot; response while processing — retry the same request until results are ready.
     */
    public LoadResponse load(RequestOptions requestOptions) {
        return this.rawClient.load(requestOptions).body();
    }

    /**
     * Runs a reporting query against the discovered schema and returns the aggregated results. Long-running queries may return a &quot;Continue wait&quot; response while processing — retry the same request until results are ready.
     */
    public LoadResponse load(LoadRequest request) {
        return this.rawClient.load(request).body();
    }

    /**
     * Runs a reporting query against the discovered schema and returns the aggregated results. Long-running queries may return a &quot;Continue wait&quot; response while processing — retry the same request until results are ready.
     */
    public LoadResponse load(LoadRequest request, RequestOptions requestOptions) {
        return this.rawClient.load(request, requestOptions).body();
    }
}
