/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.squareup.square.core.ClientOptions;
import com.squareup.square.core.MediaTypes;
import com.squareup.square.core.ObjectMappers;
import com.squareup.square.core.QueryStringMapper;
import com.squareup.square.core.RequestOptions;
import com.squareup.square.core.SquareApiException;
import com.squareup.square.core.SquareClientHttpResponse;
import com.squareup.square.core.SquareException;
import com.squareup.square.core.SyncPagingIterable;
import com.squareup.square.types.CancelTransferOrderRequest;
import com.squareup.square.types.CancelTransferOrderResponse;
import com.squareup.square.types.CreateTransferOrderRequest;
import com.squareup.square.types.CreateTransferOrderResponse;
import com.squareup.square.types.DeleteTransferOrderResponse;
import com.squareup.square.types.DeleteTransferOrdersRequest;
import com.squareup.square.types.GetTransferOrdersRequest;
import com.squareup.square.types.ReceiveTransferOrderRequest;
import com.squareup.square.types.ReceiveTransferOrderResponse;
import com.squareup.square.types.RetrieveTransferOrderResponse;
import com.squareup.square.types.SearchTransferOrdersRequest;
import com.squareup.square.types.SearchTransferOrdersResponse;
import com.squareup.square.types.StartTransferOrderRequest;
import com.squareup.square.types.StartTransferOrderResponse;
import com.squareup.square.types.TransferOrder;
import com.squareup.square.types.UpdateTransferOrderRequest;
import com.squareup.square.types.UpdateTransferOrderResponse;
import java.io.IOException;
import java.util.Collections;
import java.util.List;
import java.util.Optional;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawTransferOrdersClient {
    protected final ClientOptions clientOptions;

    public RawTransferOrdersClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Creates a new transfer order in <a href="entity:TransferOrderStatus">DRAFT</a> status. A transfer order represents the intent
     * to move <a href="entity:CatalogItemVariation">CatalogItemVariation</a>s from one <a href="entity:Location">Location</a> to another.
     * The source and destination locations must be different and must belong to your Square account.
     * <p>In <a href="entity:TransferOrderStatus">DRAFT</a> status, you can:</p>
     * <ul>
     * <li>Add or remove items</li>
     * <li>Modify quantities</li>
     * <li>Update shipping information</li>
     * <li>Delete the entire order via <a href="api-endpoint:TransferOrders-DeleteTransferOrder">DeleteTransferOrder</a></li>
     * </ul>
     * <p>The request requires source_location_id and destination_location_id.
     * Inventory levels are not affected until the order is started via
     * <a href="api-endpoint:TransferOrders-StartTransferOrder">StartTransferOrder</a>.</p>
     * <p>Common integration points:</p>
     * <ul>
     * <li>Sync with warehouse management systems</li>
     * <li>Automate regular stock transfers</li>
     * <li>Initialize transfers from inventory optimization systems</li>
     * </ul>
     * <p>Creates a <a href="webhook:transfer_order.created">transfer_order.created</a> webhook event.</p>
     */
    public SquareClientHttpResponse<CreateTransferOrderResponse> create(CreateTransferOrderRequest request) {
        return create(request, null);
    }

    /**
     * Creates a new transfer order in <a href="entity:TransferOrderStatus">DRAFT</a> status. A transfer order represents the intent
     * to move <a href="entity:CatalogItemVariation">CatalogItemVariation</a>s from one <a href="entity:Location">Location</a> to another.
     * The source and destination locations must be different and must belong to your Square account.
     * <p>In <a href="entity:TransferOrderStatus">DRAFT</a> status, you can:</p>
     * <ul>
     * <li>Add or remove items</li>
     * <li>Modify quantities</li>
     * <li>Update shipping information</li>
     * <li>Delete the entire order via <a href="api-endpoint:TransferOrders-DeleteTransferOrder">DeleteTransferOrder</a></li>
     * </ul>
     * <p>The request requires source_location_id and destination_location_id.
     * Inventory levels are not affected until the order is started via
     * <a href="api-endpoint:TransferOrders-StartTransferOrder">StartTransferOrder</a>.</p>
     * <p>Common integration points:</p>
     * <ul>
     * <li>Sync with warehouse management systems</li>
     * <li>Automate regular stock transfers</li>
     * <li>Initialize transfers from inventory optimization systems</li>
     * </ul>
     * <p>Creates a <a href="webhook:transfer_order.created">transfer_order.created</a> webhook event.</p>
     */
    public SquareClientHttpResponse<CreateTransferOrderResponse> create(
            CreateTransferOrderRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/transfer-orders")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SquareException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SquareClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), CreateTransferOrderResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SquareApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SquareException("Network error executing HTTP request", e);
        }
    }

    /**
     * Searches for transfer orders using filters. Returns a paginated list of matching
     * <a href="entity:TransferOrder">TransferOrder</a>s sorted by creation date.
     * <p>Common search scenarios:</p>
     * <ul>
     * <li>Find orders for a source <a href="entity:Location">Location</a></li>
     * <li>Find orders for a destination <a href="entity:Location">Location</a></li>
     * <li>Find orders in a particular <a href="entity:TransferOrderStatus">TransferOrderStatus</a></li>
     * </ul>
     */
    public SquareClientHttpResponse<SyncPagingIterable<TransferOrder>> search() {
        return search(SearchTransferOrdersRequest.builder().build());
    }

    /**
     * Searches for transfer orders using filters. Returns a paginated list of matching
     * <a href="entity:TransferOrder">TransferOrder</a>s sorted by creation date.
     * <p>Common search scenarios:</p>
     * <ul>
     * <li>Find orders for a source <a href="entity:Location">Location</a></li>
     * <li>Find orders for a destination <a href="entity:Location">Location</a></li>
     * <li>Find orders in a particular <a href="entity:TransferOrderStatus">TransferOrderStatus</a></li>
     * </ul>
     */
    public SquareClientHttpResponse<SyncPagingIterable<TransferOrder>> search(SearchTransferOrdersRequest request) {
        return search(request, null);
    }

    /**
     * Searches for transfer orders using filters. Returns a paginated list of matching
     * <a href="entity:TransferOrder">TransferOrder</a>s sorted by creation date.
     * <p>Common search scenarios:</p>
     * <ul>
     * <li>Find orders for a source <a href="entity:Location">Location</a></li>
     * <li>Find orders for a destination <a href="entity:Location">Location</a></li>
     * <li>Find orders in a particular <a href="entity:TransferOrderStatus">TransferOrderStatus</a></li>
     * </ul>
     */
    public SquareClientHttpResponse<SyncPagingIterable<TransferOrder>> search(
            SearchTransferOrdersRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/transfer-orders/search")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SquareException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                SearchTransferOrdersResponse parsedResponse =
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), SearchTransferOrdersResponse.class);
                Optional<String> startingAfter = parsedResponse.getCursor();
                SearchTransferOrdersRequest nextRequest = SearchTransferOrdersRequest.builder()
                        .from(request)
                        .cursor(startingAfter)
                        .build();
                List<TransferOrder> result = parsedResponse.getTransferOrders().orElse(Collections.emptyList());
                return new SquareClientHttpResponse<>(
                        new SyncPagingIterable<TransferOrder>(
                                startingAfter.isPresent(), result, () -> search(nextRequest, requestOptions)
                                        .body()),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SquareApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SquareException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieves a specific <a href="entity:TransferOrder">TransferOrder</a> by ID. Returns the complete
     * order details including:
     * <ul>
     * <li>Basic information (status, dates, notes)</li>
     * <li>Line items with ordered and received quantities</li>
     * <li>Source and destination <a href="entity:Location">Location</a>s</li>
     * <li>Tracking information (if available)</li>
     * </ul>
     */
    public SquareClientHttpResponse<RetrieveTransferOrderResponse> get(GetTransferOrdersRequest request) {
        return get(request, null);
    }

    /**
     * Retrieves a specific <a href="entity:TransferOrder">TransferOrder</a> by ID. Returns the complete
     * order details including:
     * <ul>
     * <li>Basic information (status, dates, notes)</li>
     * <li>Line items with ordered and received quantities</li>
     * <li>Source and destination <a href="entity:Location">Location</a>s</li>
     * <li>Tracking information (if available)</li>
     * </ul>
     */
    public SquareClientHttpResponse<RetrieveTransferOrderResponse> get(
            GetTransferOrdersRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/transfer-orders")
                .addPathSegment(request.getTransferOrderId())
                .build();
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SquareClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), RetrieveTransferOrderResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SquareApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SquareException("Network error executing HTTP request", e);
        }
    }

    /**
     * Updates an existing transfer order. This endpoint supports sparse updates,
     * allowing you to modify specific fields without affecting others.
     * <p>Creates a <a href="webhook:transfer_order.updated">transfer_order.updated</a> webhook event.</p>
     */
    public SquareClientHttpResponse<UpdateTransferOrderResponse> update(UpdateTransferOrderRequest request) {
        return update(request, null);
    }

    /**
     * Updates an existing transfer order. This endpoint supports sparse updates,
     * allowing you to modify specific fields without affecting others.
     * <p>Creates a <a href="webhook:transfer_order.updated">transfer_order.updated</a> webhook event.</p>
     */
    public SquareClientHttpResponse<UpdateTransferOrderResponse> update(
            UpdateTransferOrderRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/transfer-orders")
                .addPathSegment(request.getTransferOrderId())
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SquareException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SquareClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), UpdateTransferOrderResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SquareApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SquareException("Network error executing HTTP request", e);
        }
    }

    /**
     * Deletes a transfer order in <a href="entity:TransferOrderStatus">DRAFT</a> status.
     * Only draft orders can be deleted. Once an order is started via
     * <a href="api-endpoint:TransferOrders-StartTransferOrder">StartTransferOrder</a>, it can no longer be deleted.
     * <p>Creates a <a href="webhook:transfer_order.deleted">transfer_order.deleted</a> webhook event.</p>
     */
    public SquareClientHttpResponse<DeleteTransferOrderResponse> delete(DeleteTransferOrdersRequest request) {
        return delete(request, null);
    }

    /**
     * Deletes a transfer order in <a href="entity:TransferOrderStatus">DRAFT</a> status.
     * Only draft orders can be deleted. Once an order is started via
     * <a href="api-endpoint:TransferOrders-StartTransferOrder">StartTransferOrder</a>, it can no longer be deleted.
     * <p>Creates a <a href="webhook:transfer_order.deleted">transfer_order.deleted</a> webhook event.</p>
     */
    public SquareClientHttpResponse<DeleteTransferOrderResponse> delete(
            DeleteTransferOrdersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/transfer-orders")
                .addPathSegment(request.getTransferOrderId());
        if (request.getVersion().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "version", request.getVersion().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SquareClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), DeleteTransferOrderResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SquareApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SquareException("Network error executing HTTP request", e);
        }
    }

    /**
     * Cancels a transfer order in <a href="entity:TransferOrderStatus">STARTED</a> or
     * <a href="entity:TransferOrderStatus">PARTIALLY_RECEIVED</a> status. Any unreceived quantities will no
     * longer be receivable and will be immediately returned to the source <a href="entity:Location">Location</a>'s inventory.
     * <p>Common reasons for cancellation:</p>
     * <ul>
     * <li>Items no longer needed at destination</li>
     * <li>Source location needs the inventory</li>
     * <li>Order created in error</li>
     * </ul>
     * <p>Creates a <a href="webhook:transfer_order.updated">transfer_order.updated</a> webhook event.</p>
     */
    public SquareClientHttpResponse<CancelTransferOrderResponse> cancel(CancelTransferOrderRequest request) {
        return cancel(request, null);
    }

    /**
     * Cancels a transfer order in <a href="entity:TransferOrderStatus">STARTED</a> or
     * <a href="entity:TransferOrderStatus">PARTIALLY_RECEIVED</a> status. Any unreceived quantities will no
     * longer be receivable and will be immediately returned to the source <a href="entity:Location">Location</a>'s inventory.
     * <p>Common reasons for cancellation:</p>
     * <ul>
     * <li>Items no longer needed at destination</li>
     * <li>Source location needs the inventory</li>
     * <li>Order created in error</li>
     * </ul>
     * <p>Creates a <a href="webhook:transfer_order.updated">transfer_order.updated</a> webhook event.</p>
     */
    public SquareClientHttpResponse<CancelTransferOrderResponse> cancel(
            CancelTransferOrderRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/transfer-orders")
                .addPathSegment(request.getTransferOrderId())
                .addPathSegments("cancel")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SquareException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SquareClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), CancelTransferOrderResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SquareApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SquareException("Network error executing HTTP request", e);
        }
    }

    /**
     * Records receipt of <a href="entity:CatalogItemVariation">CatalogItemVariation</a>s for a transfer order.
     * This endpoint supports partial receiving - you can receive items in multiple batches.
     * <p>For each line item, you can specify:</p>
     * <ul>
     * <li>Quantity received in good condition (added to destination inventory with <a href="entity:InventoryState">InventoryState</a> of IN_STOCK)</li>
     * <li>Quantity damaged during transit/handling (added to destination inventory with <a href="entity:InventoryState">InventoryState</a> of WASTE)</li>
     * <li>Quantity canceled (returned to source location's inventory)</li>
     * </ul>
     * <p>The order must be in <a href="entity:TransferOrderStatus">STARTED</a> or <a href="entity:TransferOrderStatus">PARTIALLY_RECEIVED</a> status.
     * Received quantities are added to the destination <a href="entity:Location">Location</a>'s inventory according to their condition.
     * Canceled quantities are immediately returned to the source <a href="entity:Location">Location</a>'s inventory.</p>
     * <p>When all items are either received, damaged, or canceled, the order moves to
     * <a href="entity:TransferOrderStatus">COMPLETED</a> status.</p>
     * <p>Creates a <a href="webhook:transfer_order.updated">transfer_order.updated</a> webhook event.</p>
     */
    public SquareClientHttpResponse<ReceiveTransferOrderResponse> receive(ReceiveTransferOrderRequest request) {
        return receive(request, null);
    }

    /**
     * Records receipt of <a href="entity:CatalogItemVariation">CatalogItemVariation</a>s for a transfer order.
     * This endpoint supports partial receiving - you can receive items in multiple batches.
     * <p>For each line item, you can specify:</p>
     * <ul>
     * <li>Quantity received in good condition (added to destination inventory with <a href="entity:InventoryState">InventoryState</a> of IN_STOCK)</li>
     * <li>Quantity damaged during transit/handling (added to destination inventory with <a href="entity:InventoryState">InventoryState</a> of WASTE)</li>
     * <li>Quantity canceled (returned to source location's inventory)</li>
     * </ul>
     * <p>The order must be in <a href="entity:TransferOrderStatus">STARTED</a> or <a href="entity:TransferOrderStatus">PARTIALLY_RECEIVED</a> status.
     * Received quantities are added to the destination <a href="entity:Location">Location</a>'s inventory according to their condition.
     * Canceled quantities are immediately returned to the source <a href="entity:Location">Location</a>'s inventory.</p>
     * <p>When all items are either received, damaged, or canceled, the order moves to
     * <a href="entity:TransferOrderStatus">COMPLETED</a> status.</p>
     * <p>Creates a <a href="webhook:transfer_order.updated">transfer_order.updated</a> webhook event.</p>
     */
    public SquareClientHttpResponse<ReceiveTransferOrderResponse> receive(
            ReceiveTransferOrderRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/transfer-orders")
                .addPathSegment(request.getTransferOrderId())
                .addPathSegments("receive")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SquareException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SquareClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), ReceiveTransferOrderResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SquareApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SquareException("Network error executing HTTP request", e);
        }
    }

    /**
     * Changes a <a href="entity:TransferOrderStatus">DRAFT</a> transfer order to <a href="entity:TransferOrderStatus">STARTED</a> status.
     * This decrements inventory at the source <a href="entity:Location">Location</a> and marks it as in-transit.
     * <p>The order must be in <a href="entity:TransferOrderStatus">DRAFT</a> status and have all required fields populated.
     * Once started, the order can no longer be deleted, but it can be canceled via
     * <a href="api-endpoint:TransferOrders-CancelTransferOrder">CancelTransferOrder</a>.</p>
     * <p>Creates a <a href="webhook:transfer_order.updated">transfer_order.updated</a> webhook event.</p>
     */
    public SquareClientHttpResponse<StartTransferOrderResponse> start(StartTransferOrderRequest request) {
        return start(request, null);
    }

    /**
     * Changes a <a href="entity:TransferOrderStatus">DRAFT</a> transfer order to <a href="entity:TransferOrderStatus">STARTED</a> status.
     * This decrements inventory at the source <a href="entity:Location">Location</a> and marks it as in-transit.
     * <p>The order must be in <a href="entity:TransferOrderStatus">DRAFT</a> status and have all required fields populated.
     * Once started, the order can no longer be deleted, but it can be canceled via
     * <a href="api-endpoint:TransferOrders-CancelTransferOrder">CancelTransferOrder</a>.</p>
     * <p>Creates a <a href="webhook:transfer_order.updated">transfer_order.updated</a> webhook event.</p>
     */
    public SquareClientHttpResponse<StartTransferOrderResponse> start(
            StartTransferOrderRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v2/transfer-orders")
                .addPathSegment(request.getTransferOrderId())
                .addPathSegments("start")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new SquareException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new SquareClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), StartTransferOrderResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new SquareApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new SquareException("Network error executing HTTP request", e);
        }
    }
}
