/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square;

import com.squareup.square.core.ClientOptions;
import com.squareup.square.core.RequestOptions;
import com.squareup.square.types.LoadRequest;
import com.squareup.square.types.LoadResponse;
import com.squareup.square.types.MetadataResponse;
import java.util.concurrent.CompletableFuture;

public class AsyncReportingClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawReportingClient rawClient;

    public AsyncReportingClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawReportingClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawReportingClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Describes the data available to query: the cubes, views, measures, dimensions, and segments you can reference in a reporting query. Call this first to discover the schema, then pass the members you need to <code>load</code>.
     */
    public CompletableFuture<MetadataResponse> getMetadata() {
        return this.rawClient.getMetadata().thenApply(response -> response.body());
    }

    /**
     * Describes the data available to query: the cubes, views, measures, dimensions, and segments you can reference in a reporting query. Call this first to discover the schema, then pass the members you need to <code>load</code>.
     */
    public CompletableFuture<MetadataResponse> getMetadata(RequestOptions requestOptions) {
        return this.rawClient.getMetadata(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Runs a reporting query against the discovered schema and returns the aggregated results. Long-running queries may return a &quot;Continue wait&quot; response while processing — retry the same request until results are ready.
     */
    public CompletableFuture<LoadResponse> load() {
        return this.rawClient.load().thenApply(response -> response.body());
    }

    /**
     * Runs a reporting query against the discovered schema and returns the aggregated results. Long-running queries may return a &quot;Continue wait&quot; response while processing — retry the same request until results are ready.
     */
    public CompletableFuture<LoadResponse> load(RequestOptions requestOptions) {
        return this.rawClient.load(requestOptions).thenApply(response -> response.body());
    }

    /**
     * Runs a reporting query against the discovered schema and returns the aggregated results. Long-running queries may return a &quot;Continue wait&quot; response while processing — retry the same request until results are ready.
     */
    public CompletableFuture<LoadResponse> load(LoadRequest request) {
        return this.rawClient.load(request).thenApply(response -> response.body());
    }

    /**
     * Runs a reporting query against the discovered schema and returns the aggregated results. Long-running queries may return a &quot;Continue wait&quot; response while processing — retry the same request until results are ready.
     */
    public CompletableFuture<LoadResponse> load(LoadRequest request, RequestOptions requestOptions) {
        return this.rawClient.load(request, requestOptions).thenApply(response -> response.body());
    }
}
