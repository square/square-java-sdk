/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.squareup.square.teammembers.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.squareup.square.core.ObjectMappers;
import com.squareup.square.types.WageSetting;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = UpdateWageSettingRequest.Builder.class)
public final class UpdateWageSettingRequest {
    private final String teamMemberId;

    private final WageSetting wageSetting;

    private final Map<String, Object> additionalProperties;

    private UpdateWageSettingRequest(
            String teamMemberId, WageSetting wageSetting, Map<String, Object> additionalProperties) {
        this.teamMemberId = teamMemberId;
        this.wageSetting = wageSetting;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The ID of the team member for which to update the <code>WageSetting</code> object.
     */
    @JsonProperty("team_member_id")
    public String getTeamMemberId() {
        return teamMemberId;
    }

    /**
     * @return The complete <code>WageSetting</code> object. For all job assignments, specify one of the following:
     * <ul>
     * <li><code>job_id</code> (recommended) - If needed, call <a href="api-endpoint:Team-ListJobs">ListJobs</a> to get a list of all jobs.
     * Requires Square API version 2024-12-18 or later.</li>
     * <li><code>job_title</code> - Use the exact, case-sensitive spelling of an existing title unless you want to create a new job.
     * This value is ignored if <code>job_id</code> is also provided.</li>
     * </ul>
     */
    @JsonProperty("wage_setting")
    public WageSetting getWageSetting() {
        return wageSetting;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof UpdateWageSettingRequest && equalTo((UpdateWageSettingRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(UpdateWageSettingRequest other) {
        return teamMemberId.equals(other.teamMemberId) && wageSetting.equals(other.wageSetting);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.teamMemberId, this.wageSetting);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static TeamMemberIdStage builder() {
        return new Builder();
    }

    public interface TeamMemberIdStage {
        /**
         * <p>The ID of the team member for which to update the <code>WageSetting</code> object.</p>
         */
        WageSettingStage teamMemberId(@NotNull String teamMemberId);

        Builder from(UpdateWageSettingRequest other);
    }

    public interface WageSettingStage {
        /**
         * <p>The complete <code>WageSetting</code> object. For all job assignments, specify one of the following:</p>
         * <ul>
         * <li><code>job_id</code> (recommended) - If needed, call <a href="api-endpoint:Team-ListJobs">ListJobs</a> to get a list of all jobs.
         * Requires Square API version 2024-12-18 or later.</li>
         * <li><code>job_title</code> - Use the exact, case-sensitive spelling of an existing title unless you want to create a new job.
         * This value is ignored if <code>job_id</code> is also provided.</li>
         * </ul>
         */
        _FinalStage wageSetting(@NotNull WageSetting wageSetting);
    }

    public interface _FinalStage {
        UpdateWageSettingRequest build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements TeamMemberIdStage, WageSettingStage, _FinalStage {
        private String teamMemberId;

        private WageSetting wageSetting;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(UpdateWageSettingRequest other) {
            teamMemberId(other.getTeamMemberId());
            wageSetting(other.getWageSetting());
            return this;
        }

        /**
         * <p>The ID of the team member for which to update the <code>WageSetting</code> object.</p>
         * <p>The ID of the team member for which to update the <code>WageSetting</code> object.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("team_member_id")
        public WageSettingStage teamMemberId(@NotNull String teamMemberId) {
            this.teamMemberId = Objects.requireNonNull(teamMemberId, "teamMemberId must not be null");
            return this;
        }

        /**
         * <p>The complete <code>WageSetting</code> object. For all job assignments, specify one of the following:</p>
         * <ul>
         * <li><code>job_id</code> (recommended) - If needed, call <a href="api-endpoint:Team-ListJobs">ListJobs</a> to get a list of all jobs.
         * Requires Square API version 2024-12-18 or later.</li>
         * <li><code>job_title</code> - Use the exact, case-sensitive spelling of an existing title unless you want to create a new job.
         * This value is ignored if <code>job_id</code> is also provided.</li>
         * </ul>
         * <p>The complete <code>WageSetting</code> object. For all job assignments, specify one of the following:</p>
         * <ul>
         * <li><code>job_id</code> (recommended) - If needed, call <a href="api-endpoint:Team-ListJobs">ListJobs</a> to get a list of all jobs.
         * Requires Square API version 2024-12-18 or later.</li>
         * <li><code>job_title</code> - Use the exact, case-sensitive spelling of an existing title unless you want to create a new job.
         * This value is ignored if <code>job_id</code> is also provided.</li>
         * </ul>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("wage_setting")
        public _FinalStage wageSetting(@NotNull WageSetting wageSetting) {
            this.wageSetting = Objects.requireNonNull(wageSetting, "wageSetting must not be null");
            return this;
        }

        @java.lang.Override
        public UpdateWageSettingRequest build() {
            return new UpdateWageSettingRequest(teamMemberId, wageSetting, additionalProperties);
        }
    }
}
